# Local Docker stack (DO NOT use production credentials)
COMPOSE_PROJECT_NAME=openclaw_local

# WordPress
WP_PORT=8080
WP_ADMIN_EMAIL=admin@local.test

# Database (MariaDB is MySQL-compatible)
DB_ROOT_PASSWORD=local_root_change_me
DB_NAME=wordpress
DB_USER=wp_user
DB_PASSWORD=wp_password_change_me
DB_PORT=3307

# phpMyAdmin (optional)
PHPMYADMIN_PORT=18081

# API callback targets for plugin configuration (set inside WP admin plugin settings)
# Example webhook URL from host machine to automation-api running locally:
# http://host.docker.internal:3000/hooks/wp-comment
